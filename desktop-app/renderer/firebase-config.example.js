// Fill this in with YOUR Firebase project's web config, then save it as
// "firebase-config.js" (not this file) in this same folder.
//
//   cp firebase-config.example.js firebase-config.js
//
// Firebase console > Project settings > General > "Your apps" > SDK setup
// and config. See ../../README.md for step-by-step instructions (takes
// about 3 minutes).
//
// IMPORTANT: this same config object must also go into
// mobile-app/firebase-config.js, so both apps talk to the same database.
//
// firebase-config.js is gitignored on purpose -- it holds your personal
// project credentials and should never be committed.

window.FIREBASE_CONFIG = {
  apiKey: "PASTE_YOUR_API_KEY",
  authDomain: "PASTE_YOUR_PROJECT.firebaseapp.com",
  databaseURL: "https://PASTE_YOUR_PROJECT-default-rtdb.firebaseio.com",
  projectId: "PASTE_YOUR_PROJECT",
  storageBucket: "PASTE_YOUR_PROJECT.appspot.com",
  messagingSenderId: "PASTE_SENDER_ID",
  appId: "PASTE_APP_ID",
};
